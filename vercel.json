{
  "version": 2,
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/node"
    },
    {
      "env": {
        "NODE_ENV": "production"
      },
      "commands": {
        "install": "npm install --legacy-peer-deps",
        "build": "npm run build"
      }
    }
  ]
}
